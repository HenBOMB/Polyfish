//! The belief's test suite, split by what each group pins:
//! `villages` the C1/C2 existence math, `capital` the C3/C4 posterior and the
//! fog discipline, `purity` the recompute-anywhere contract, and `parity` the
//! frozen pre-SSOT behaviour the migration must not have moved.

mod capital;
mod fixtures;
mod parity;
mod purity;
mod villages;
