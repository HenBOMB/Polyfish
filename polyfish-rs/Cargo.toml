[package]
name = "polyfish"
version = "0.1.0"
edition = "2021"
description = "Polytopia game simulator in Rust"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_repr = "0.1"
rand = "0.8"

[dev-dependencies]
pretty_assertions = "1.4"
